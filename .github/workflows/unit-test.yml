name: Unit Tests

on:
  pull_request:
    branches: [ "main" ]

jobs:
  Ubuntu:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Dependencies (Ubuntu)
      run: sudo apt-get install libopencv-dev valgrind
    - name: Test (Ubuntu)
      run: ./test.sh
  Mac:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v3
    - name: Dependencies (Mac)
      run: brew install opencv llvm; echo 'alias clang++="/usr/local/opt/llvm/bin/clang++"' >> ~/.bash_profile; source ~/.bash_profile
    - name: Test (Mac)
      run: ./test.sh
